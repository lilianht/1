name: Saved new search
source: marketplace
type: raw_sql
rawSql: |
  SELECT
    USER_ID,
    DATEADD(DAY, -NUMBER_7, CURRENT_TIMESTAMP()) AS CREATED_AT,
    USER_ID AS SEARCH_ID,
  FROM
    PUBLIC.EVENTS;
eventTimestampColumn: CREATED_AT
